# Human tasks

When the process needs something only a human can do, add it here. Mark done when complete.

- [ ] **Phase 0:** Ensure `git config user.email` and `git config user.name` are set in the repo (if using git).
- [ ] *(Add more tasks as needed, e.g. "Set OPENAI_API_KEY for deploy", "Approve go-live")*
