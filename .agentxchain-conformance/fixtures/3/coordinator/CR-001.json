{
  "fixture_id": "CR-001",
  "tier": 3,
  "surface": "coordinator",
  "description": "Valid coordinator config with repos and workstreams validates successfully",
  "type": "validate",
  "setup": {
    "coordinator_config": {
      "schema_version": "0.1",
      "project": { "id": "multi-test", "name": "Multi Repo Test" },
      "repos": {
        "api": { "path": "./repos/api", "required": true },
        "web": { "path": "./repos/web", "required": true }
      },
      "workstreams": {
        "backend": {
          "phase": "implementation",
          "repos": ["api"],
          "entry_repo": "api",
          "depends_on": [],
          "completion_barrier": "all_repos_accepted"
        },
        "frontend": {
          "phase": "implementation",
          "repos": ["web"],
          "entry_repo": "web",
          "depends_on": ["backend"],
          "completion_barrier": "all_repos_accepted"
        }
      }
    },
    "repos": {
      "api": {
        "path": "./repos/api",
        "config": {
          "schema_version": "1.0",
          "project": { "id": "api", "name": "API" },
          "roles": { "dev": { "runtime": "manual" } },
          "runtimes": { "manual": { "type": "manual" } }
        }
      },
      "web": {
        "path": "./repos/web",
        "config": {
          "schema_version": "1.0",
          "project": { "id": "web", "name": "Web" },
          "roles": { "dev": { "runtime": "manual" } },
          "runtimes": { "manual": { "type": "manual" } }
        }
      }
    }
  },
  "input": {
    "operation": "validate_coordinator_config",
    "args": {}
  },
  "expected": {
    "result": "success",
    "errors": []
  }
}
