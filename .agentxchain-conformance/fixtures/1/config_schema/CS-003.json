{
  "fixture_id": "CS-003",
  "tier": 1,
  "surface": "config_schema",
  "description": "Role references undeclared runtime rejected",
  "type": "reject",
  "setup": {},
  "input": {
    "operation": "validate_config",
    "args": {
      "config": {
        "schema_version": "1.0",
        "project": {
          "id": "test-project",
          "name": "Test Project"
        },
        "roles": {
          "dev": {
            "title": "Developer",
            "mandate": "Build features",
            "write_authority": "authoritative",
            "runtime": "cloud-runner"
          }
        },
        "runtimes": {
          "local-dev": {
            "type": "local_cli"
          }
        },
        "routing": {
          "implementation": {
            "entry_role": "dev",
            "allowed_next_roles": ["dev", "human"],
            "max_concurrent_turns": 1
          }
        }
      }
    }
  },
  "expected": {
    "result": "error",
    "error_type": "undeclared_runtime_reference",
    "error_field": "roles.dev.runtime",
    "referenced_runtime": "cloud-runner"
  }
}
